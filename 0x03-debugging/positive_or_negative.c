#include <stdio.h>

/**
 * positive_or_negative - checks if an integer is positive, zero
 * or negative
 */

void positive_or_negative(int i)
{
	if (i > 0)
		printf("%d is positive\n", i);
	else if (i == 0)
		printf("%d is zero\n", i);
	else if (i < 0)
		printf("%d is negative\n", i);
}
